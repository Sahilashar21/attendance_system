# System Architecture & Data Flow

## 🏗️ Overall System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      USER INTERFACE (Browser)                    │
│                  HTML Templates + Bootstrap 5                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │  Admin Login │  │ Intern Login │  │  Public Views        │  │
│  └──────┬───────┘  └──────┬───────┘  │  (Limited Access)    │  │
│         │                  │          └──────────────────────┘  │
│         └──────────────────┴──────────────────────────────────── │
│                            │                                     │
├─────────────────────────────────────────────────────────────────┤
│              FLASK APPLICATION LAYER (Python)                    │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Authentication & Authorization (@login_required)      │   │
│  └────────────────┬────────────────────────────────────────┘   │
│                   │                                              │
│  ┌────────┬───────┴───────┬────────────┬────────┬──────────┐   │
│  │        │               │            │        │          │   │
│  ▼        ▼               ▼            ▼        ▼          ▼   │
│ Login   Admin         Intern         Leave    Holiday   Attendance
│ Routes  Dashboard     Dashboard     Management Management Functions
│         Routes       Routes        Routes     Routes              │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Business Logic Layer                                    │   │
│  │  - Hour calculations                                     │   │
│  │  - Leave validation                                      │   │
│  │  - Calendar logic                                        │   │
│  │  - Access control                                        │   │
│  └──────────────┬───────────────────────────────────────────┘   │
│                 │                                                │
├─────────────────────────────────────────────────────────────────┤
│              MONGODB DATABASE LAYER                              │
│                                                                   │
│  ┌──────────┐  ┌────────────┐  ┌────────┐  ┌──────────┐        │
│  │  users   │  │ attendance │  │ leaves │  │ holidays │        │
│  │collection│  │ collection │  │collect.│  │ collect. │        │
│  └──────────┘  └────────────┘  └────────┘  └──────────┘        │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Feature-Specific Data Flows

### Feature 1: Add Admin

```
┌─────────────────────┐
│  Admin Dashboard    │
│  "Add Admin" Button │
└──────────┬──────────┘
           │
           ▼
   ┌──────────────────┐
   │  Form Submission │
   │ (POST /admin/add-│
   │ admin)           │
   └──────────┬───────┘
              │
              ▼
      ┌───────────────────┐
      │ Input Validation  │
      │ - Username unique │
      │ - All fields set  │
      └─────────┬─────────┘
                │
        ┌───────┴────────┐
        │                │
        ▼                ▼
    ✅ VALID        ❌ INVALID
        │                │
        ▼                ▼
   INSERT into      Return Error
   users table      Message
   (role: admin)
        │
        ▼
   ✅ Success Message
   Redirect to
   Dashboard
```

---

### Feature 2: Change Work Type

```
┌──────────────────────────┐
│ Admin Dashboard          │
│ Attendance Record Row    │
└──────────┬───────────────┘
           │
           ▼
   ┌──────────────────────┐
   │ Click Edit Button    │
   │ (Expand Form)        │
   └──────────┬───────────┘
              │
              ▼
      ┌───────────────────┐
      │ Work Type Dropdown│
      │ Office / WFH      │
      └──────────┬────────┘
                 │
                 ▼
       ┌─────────────────┐
       │ Select New Type │
       └─────────┬───────┘
                 │
                 ▼
    ┌────────────────────────┐
    │ Auto-submit Form       │
    │ (POST /admin/change-   │
    │ work-type)             │
    └────────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Update Database    │
        │ attendance.update  │
        │ ({work_type: new}) │
        └────────────┬───────┘
                     │
                     ▼
              ✅ Success
              Table Refreshes
              New Work Type
              Displayed
```

---

### Feature 3: Edit Times

```
┌──────────────────────────┐
│ Admin Dashboard          │
│ Present Record Row       │
└──────────┬───────────────┘
           │
           ▼
   ┌──────────────────────┐
   │ Click Edit Button    │
   └──────────┬───────────┘
              │
              ▼
      ┌───────────────────┐
      │ Edit Form Expands │
      │ Login Time field  │
      │ Logout Time field │
      └──────────┬────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Enter New Times    │
        │ Format: HH:MM:SS   │
        └─────────┬──────────┘
                  │
                  ▼
    ┌─────────────────────────┐
    │ Click Save Changes      │
    │ (POST /admin/edit-time) │
    └────────────┬────────────┘
                 │
                 ▼
        ┌────────────────────────┐
        │ Validate Times         │
        │ Calculate Hours        │
        │ Handle Overnight Shift │
        │ (add 24h if negative)  │
        └─────────┬──────────────┘
                  │
                  ▼
        ┌────────────────────────┐
        │ Update Attendance:      │
        │ - login_time           │
        │ - logout_time          │
        │ - hours_worked (auto)  │
        └─────────┬──────────────┘
                  │
                  ▼
           ✅ Success
           Hours Refreshed
           Report Updated
```

---

### Feature 4: Leave Management Flow

```
┌──────────────────────────┐
│ Intern Dashboard         │
│ Leaves Tab              │
└──────────┬───────────────┘
           │
           ▼
    ┌──────────────────┐
    │ Apply for Leave  │
    │ - Start Date     │
    │ - End Date       │
    │ - Reason         │
    └─────────┬────────┘
              │
              ▼
   ┌──────────────────────────┐
   │ Submit Form              │
   │ (POST /intern/apply-     │
   │ leave)                   │
   └──────────┬───────────────┘
              │
              ▼
     ┌────────────────────┐
     │ Create Leave Req   │
     │ status: "Pending"  │
     └──────────┬─────────┘
                │
                ▼
   ┌────────────────────────────┐
   │ Admin Dashboard            │
   │ "Leave Requests" Section   │
   │ Shows Pending Requests     │
   └──────────┬─────────────────┘
              │
        ┌─────┴──────┐
        │            │
        ▼            ▼
    ✅ Approve   ❌ Reject
        │            │
        ▼            ▼
   ┌──────────────┐  │
   │ For each day │  │
   │ 1-N of leave:│  │
   │              │  │
   │ Create       │  │ Leave status
   │ Attendance:  │  │ changes to
   │ - date       │  │ "Rejected"
   │ - work_type: │  │ No records
   │   "Leave"    │  │ created
   │ - status:    │  │
   │   "Leave-    │  │
   │   Approved"  │  │
   │ - leave_id   │  │
   └────────┬─────┘  │
            │        │
            └────┬───┘
                 │
                 ▼
         ✅ Leave Status Updated
         Calendar Updated
         Login Blocked on Leave
```

---

### Feature 5: Block Login on Approved Leave

```
┌──────────────────────────┐
│ Intern Dashboard         │
│ Try to Mark Login        │
└──────────┬───────────────┘
           │
           ▼
    ┌──────────────────────┐
    │ Check Approved Leave │
    │ Query leaves_col     │
    │ (Approved + date)    │
    └──────────┬───────────┘
              │
        ┌─────┴──────────┐
        │                │
        ▼                ▼
    FOUND          NOT FOUND
        │                │
        ▼                ▼
    ❌ Error        Continue
    "You have      Checks
    confirmed
    leave today!"
        │
        ▼
    Cannot Mark
    Attendance
    Redirect to
    Dashboard
```

---

### Feature 6: Calendar Color Logic

```
                 ┌─────────────────────┐
                 │ Render Calendar Day │
                 └──────────┬──────────┘
                            │
                            ▼
                 ┌──────────────────────────┐
                 │ Check Attendance Record  │
                 │ for this date            │
                 └─────────┬────────────────┘
                           │
                      ┌────┴────┐
                      │          │
                      ▼          ▼
                   EXISTS    NOT EXISTS
                      │          │
                      ▼          ▼
              ┌──────────────┐  Check
              │Check work_   │  Approved
              │type field    │  Leave?
              └──┬────┬───┬──┘  │
                 │    │   │     └──┬────┐
         "home"  │    │   │"other" │YES │ NO
                 ▼    ▼   ▼        │    │
            YELLOW GREEN GREEN     ▼    ▼
                                  RED  Check
                                       Holiday?
                                        │
                                    ┌───┴────┐
                                    │        │
                                   YES      NO
                                    │        │
                                    ▼        ▼
                                  GREEN  Check
                                         Past?
                                          │
                                      ┌───┴────┐
                                     YES      NO
                                      │        │
                                      ▼        ▼
                                     RED      NONE
                                    (Absent)
```

---

## 📊 Database Relationships

```
┌──────────────────┐
│     users        │
│  (_id, role)     │
└────────┬─────────┘
         │
    1:N relationship
         │
         ├──────────────────────────────────┐
         │                                  │
         ▼                                  ▼
┌────────────────────┐           ┌────────────────────┐
│   attendance       │           │     leaves         │
│ (user_id, date)    │           │ (user_id, date)    │
└────────┬───────────┘           └──────────┬─────────┘
         │                               │
         │ leave_id FK                   │
         └───────────────────────────────┘
         (When leave approved)

Holidays stand alone:
┌──────────────────┐
│   holidays       │
│ (date, name)     │
└──────────────────┘
(No direct relationships)
```

---

## 🔐 Security & Access Control Flow

```
┌──────────────────────┐
│ User Request         │
│ (GET/POST)           │
└──────────┬───────────┘
           │
           ▼
   ┌───────────────────┐
   │ Check Session ID  │
   │ (login_required)  │
   └─────────┬─────────┘
             │
        ┌────┴─────┐
        │          │
        ▼          ▼
    VALID     INVALID
        │          │
        ▼          ▼
   Continue   Redirect
   Check      to Login
   Role?      Page
        │
    ┌───┴─────┐
    │         │
    ▼         ▼
  ADMIN    INTERN
    │         │
    │ Check   │ Check
    │ role    │ role
    │ match?  │ match?
    │         │
┌───┴──┐  ┌──┴──┐
│      │  │     │
YES   NO YES   NO
│      │  │     │
▼      ▼  ▼     ▼
✅    ❌ ✅    ❌
Allow Deny Allow Deny
```

---

## 🔄 Async/Background Operations

```
Currently: No async operations
(All operations synchronous)

Recommended Future Enhancements:
├─ Email notifications (async)
├─ Report generation (async/background job)
└─ Bulk import processing (async)
```

---

## 📱 Response Flow Chart

```
Browser Request
      │
      ▼
┌──────────────────┐
│ Flask Route      │
│ Handler          │
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
Success   Error
    │         │
    ▼         ▼
 Render    Flash
 Template  Message
    │         │
    ▼         ▼
Return    Redirect
 HTML    or Error
    │         │
    └────┬────┘
         │
         ▼
   Browser
  Displays
   Page
```

---

## 🎯 Deployment Architecture

```
┌─────────────────────────────────────┐
│ Production Server (Linux/Windows)   │
│                                     │
│ ┌──────────────────────────────┐   │
│ │ Python Virtual Environment   │   │
│ │ - Flask 3.1.2               │   │
│ │ - pymongo 4.6.3             │   │
│ │ - pytz, pandas, etc.        │   │
│ └────────────┬─────────────────┘   │
│              │                      │
│ ┌────────────▼──────────────────┐  │
│ │ Flask Application (app.py)    │  │
│ │ - Port: 5000 (default)        │  │
│ │ - Hot reload: disabled        │  │
│ │ - Debug: disabled             │  │
│ └────────────┬──────────────────┘  │
│              │                      │
└──────────────┼──────────────────────┘
               │
         ┌─────▼──────┐
         │ MongoDB    │
         │ Connection │
         └─────┬──────┘
               │
         ┌─────▼──────┐
         │ Local DB   │
         │ or Atlas   │
         └────────────┘
```

---

## 📈 Load Balancing (Future)

```
┌─────────────────────────────────────┐
│ Load Balancer (Nginx/HAProxy)      │
└───────┬─────────────────────────────┘
        │
    ┌───┴─────┬──────────┐
    │         │          │
    ▼         ▼          ▼
  Flask    Flask      Flask
 Server 1 Server 2  Server N
    │         │          │
    └────┬────┴──────────┘
         │
    ┌────▼──────────┐
    │ MongoDB (in   │
    │ replication   │
    │ set)          │
    └───────────────┘
```

---

## 🚀 Scaling Strategy

### Current (Single Server):
- Single Flask instance
- Single MongoDB instance
- Suitable for: Small to medium organizations

### Recommended Future:
1. **Vertical Scaling**
   - Increase server resources
   - Optimize database queries
   - Cache frequently accessed data

2. **Horizontal Scaling**
   - Multiple Flask instances
   - Load balancer
   - MongoDB replication

3. **Database Scaling**
   - Sharding by department
   - Replication set
   - Atlas auto-scaling

---

This architecture supports all the new features while maintaining:
- ✅ Security
- ✅ Scalability
- ✅ Maintainability
- ✅ Performance
- ✅ Reliability
